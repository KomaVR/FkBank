document.addEventListener('DOMContentLoaded', function() {
    // Simulate account balance
    const balance = 1000.00; // Example value
    document.getElementById('balance').textContent = balance.toFixed(2);
});
